# 阶段 1：生产地基(多租户 + 持久化)
fastapi>=0.115
uvicorn[standard]>=0.32
anthropic>=0.40
pydantic-settings>=2.6
python-dotenv>=1.0

# 数据层(多租户 Postgres + RLS)
sqlalchemy[asyncio]>=2.0
psycopg[binary]>=3.2        # Python 3.14 wheel 兼容比 asyncpg 好
alembic>=1.13
cryptography>=43            # 加密存储各组织的模型 API key
